# ============================================================================
# Example .env.local for Local Development
# ============================================================================
#
# Copy this file to .env.local and fill in your values.
# .env.local is in .gitignore and should never be committed!
#
# For Docker Compose local development:
# - Database: PostgreSQL running in container
# - Email: Mailgun (use sandbox domain for testing)
# - Host: http://localhost:3000
#

# Database (via Docker Compose)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/authforge

# Development
NODE_ENV=development
PORT=3000

# Application
HOST_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# JWT (Generate: openssl rand -base64 32)
JWT_SECRET=your_development_jwt_secret_here_min_32_chars

# Email Service (Mailgun Sandbox)
# For testing, use Mailgun sandbox domain
# Domain: mg.yourdomain.mailgun.org (sandbox)
MAILGUN_API_KEY=your_mailgun_sandbox_api_key
MAILGUN_DOMAIN=mg.yourdomain.mailgun.org
FROM_EMAIL=postmaster@mg.yourdomain.mailgun.org

# CORS (allow all origins in development)
ALLOWED_ORIGINS=

# Docker Compose Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=authforge
DB_PORT=5432

# Optional: Logging
LOG_LEVEL=debug
